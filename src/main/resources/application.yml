spring:
  application:
    name: nut-ai-agent
  ai:
    dashscope:
      api-key: sk-4c29a2299e8c4924b514861b90f4f036
      chat:
        options:
          model: qwen-plus
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.xqj.nutaiagent.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn